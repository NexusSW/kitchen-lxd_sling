require "spec_helper"

RSpec.describe Kitchen::Driver::LXD do
  it "has a version number" do
    expect(Kitchen::Driver::LXD::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
